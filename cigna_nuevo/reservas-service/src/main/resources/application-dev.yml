# Perfil de desarrollo local
spring:
  datasource:
    url: "jdbc:mysql://host.docker.internal:3306/bd_reservas?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true"
    username: root
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.example: DEBUG
    org.springframework.web: DEBUG
